# docker-compose.override.yml - Development only
# This file is applied automatically when NODE_ENV != production

version: '3.8'

services:
  web-1:
    volumes:
      - ./app:/code/app  # Hot reload for development
    environment:
      - DEBUG=true
      - ENVIRONMENT=development
  web-2:
    volumes:
      - ./app:/code/app  # Hot reload for development
    environment:
      - DEBUG=true
      - ENVIRONMENT=development
  web-3:
    volumes:
      - ./app:/code/app  # Hot reload for development
    environment:
      - DEBUG=true
      - ENVIRONMENT=development