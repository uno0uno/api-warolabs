issue: 339
title: "feat(api): promotion list scope summary fields (count + name preview)"
repo: api_warocol.com
repo_remote: uno0uno/api-warolabs
cwd: /Users/saifer/Documents/WEBS/WARO COLOMBIA/api_warocol.com
stack: fastapi
branch: wr-339-promo-scope-summary

paths:
  - app/services/promotions_service.py
  - tests/test_promotions_crud.py

ac:
  - list_promotions rows include product_count, category_count
  - product_names_preview and category_names_preview max 3, stable name order
  - full product_ids / category_ids unchanged
  - tests for products, categories, all_products scopes

out_of_scope:
  - GET /promotions/{id}/scope paginated endpoint (warocol.com#999)
  - POS badge and promotion panel UI

hints:
  entry: "promotions_service._serialize_promotion — extend row payload"
  pattern: "promotions_service.py:175 — batch JOIN scope tables for names"
  tests: "pytest tests/test_promotions_crud.py -v"

skip:
  audits: [ux, color, typography]
